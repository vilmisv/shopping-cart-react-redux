const selectList = ({ list }) => list

const selectCart = ({ cart }) => cart

const selectSearch = ({ searchField}) => searchField

const selectFilteredCategory = ({ filteredItems }) => filteredItems




const selectors = {
    selectList,
    selectCart,
    selectSearch,
    selectFilteredCategory,
}
export default selectors